# ============================================
# Premium Welding & Fabrication CRM
# Environment Variables Template
# ============================================
# 
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to git (it's already in .gitignore)
#
# ============================================

# ============================================
# Supabase Configuration (Required)
# ============================================
# Get these from your Supabase project dashboard:
# https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
#
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================
# Document Generation Service (Optional)
# ============================================
# For local development: Use local Python service
# For production: Uses Vercel serverless function at /api/generate
# Only set this if you're running the Python service locally for testing
#
VITE_DOCX_SERVICE_URL=http://localhost:5001/generate

# ============================================
# Service Role Key (For Scripts Only)
# ============================================
# ⚠️  WARNING: This is a SECRET key - NEVER expose to frontend!
# This is only used by Node.js scripts (migrations, data fixes, etc.)
# Get from: Supabase Dashboard → Settings → API → service_role key
#
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# Logging Configuration (Optional)
# ============================================
# Log levels: debug, info, warn, error
# Default: debug in development, error in production
# Set this to control what logs appear in the browser console
#
VITE_LOG_LEVEL=debug

# ============================================
# Notes
# ============================================
# - All VITE_* variables are exposed to the frontend bundle
# - Never put secrets in VITE_* variables
# - SUPABASE_SERVICE_ROLE_KEY is only for server-side scripts
# - VITE_DOCX_SERVICE_URL is optional (defaults to /api/generate in production)
# - VITE_LOG_LEVEL is optional (defaults based on environment)
